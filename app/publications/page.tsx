import { displayNumber, getPublicationsContent } from "@/lib/content";
const links={primary:"text-sm font-medium text-[#9a742d] hover:text-[#76571e]",secondary:"text-sm font-medium text-[#62676f] hover:text-[#202033]"};
export default async function Publications(){const {page,profile,items}=await getPublicationsContent();return <main className="site-shell bg-dot-paper"><div className="page-wrap">
<p className="editorial-kicker">{page.eyebrow}</p><h1 className="editorial-title">{page.title}{page.titleMuted&&<><br/><span className="editorial-title-muted">{page.titleMuted}</span></>}</h1><p className="mt-8 max-w-2xl text-lg leading-8 text-[#62676f]">{page.intro}</p>
<div className="mt-16 divide-y divide-[#d7cdb9] border-y border-[#d7cdb9]">{items.map((p,i)=><article key={p.id} className="py-9"><div className="grid gap-6 md:grid-cols-[5rem_1fr_auto]"><span className="font-display text-2xl text-[#a5a397]">{displayNumber(i)}</span><div><p className="editorial-kicker">{p.category}</p><h2 className="mt-2 font-display text-2xl leading-tight md:text-3xl">{p.title}</h2><p className="mt-3 text-sm text-[#777b86]">{p.byline}</p><p className="mt-5 max-w-3xl leading-7 text-[#62676f]">{p.summary}</p><div className="mt-5 flex flex-wrap gap-2">{p.tags.map(t=><span key={t} className="rounded-full border border-[#d7cdb9] px-3 py-1.5 text-xs text-[#62676f]">{t}</span>)}</div></div><span className="text-xs text-[#777b86]">{p.status}</span></div>{p.links.length>0&&<div className="mt-7 flex gap-5 border-t border-[#d7cdb9] pt-5">{p.links.map(l=><a key={l.label} href={l.url} target="_blank" rel="noopener noreferrer" className={links[l.variant]}>{l.label} ↗</a>)}</div>}</article>)}</div>
<div className="mt-12 border-l-2 border-[#9a742d] bg-[#eee7d4] p-7"><p className="editorial-kicker">{profile.heading}</p><p className="mt-3 max-w-3xl leading-7 text-[#62676f]">{profile.text}</p></div>
</div></main>}