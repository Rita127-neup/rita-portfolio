import { getContactContent,getSiteSettings } from "@/lib/content";
export default async function Contact(){const [c,s]=await Promise.all([getContactContent(),getSiteSettings()]);return <main className="site-shell bg-dot-paper"><div className="page-wrap"><p className="editorial-kicker">{c.eyebrow}</p><h1 className="editorial-title">{c.title}</h1><p className="mt-8 max-w-2xl text-lg leading-8 text-[#62676f]">{c.intro}</p><div className="mt-16 grid gap-8 md:grid-cols-2"><section className="editorial-card rounded-[1.5rem] p-8 md:p-10"><p className="editorial-kicker">{c.emailCardTitle}</p><p className="mt-4 leading-7 text-[#62676f]">{c.emailCardText}</p><a href={`mailto:${s.contactEmail}`} className="mt-8 inline-block text-sm font-semibold text-[#9a742d] underline underline-offset-8">Email me →</a></section><section className="editorial-card rounded-[1.5rem] p-8 md:p-10"><p className="editorial-kicker">{c.socialCardTitle}</p><div className="mt-5 divide-y divide-[#d7cdb9] border-y border-[#d7cdb9]">{s.socialLinks.map(x=>x.url?<a key={x.label} href={x.url} target="_blank" rel="noopener noreferrer" className="block py-4 text-sm font-medium hover:text-[#9a742d]">{x.label} ↗</a>:<div key={x.label} className="py-4 text-sm text-[#777b86]">{x.label} — coming soon</div>)}</div></section></div></div></main>}